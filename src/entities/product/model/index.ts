import { atom } from 'jotai'

export const productCollectionType = atom<number>(0)
export const productCollectionBrand = atom<number | null>(null)
export const productCollectionModel = atom<number | null>(null)
